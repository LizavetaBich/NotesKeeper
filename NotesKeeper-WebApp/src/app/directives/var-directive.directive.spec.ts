import { VarDirective } from './var-directive.directive';

describe('VarDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new VarDirective();
    expect(directive).toBeTruthy();
  });
});
